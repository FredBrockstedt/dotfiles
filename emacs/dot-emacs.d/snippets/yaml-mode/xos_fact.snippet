# -*- mode: snippet -*-
# name: Collect facts from devices running Extreme SLX-OS
# prefix: slxos_facts_snippet
# key: xos_fact
# group: ansible
# --

- name: ${0:Collect facts from devices running Extreme SLX-OS}
  slxos_facts:                                                             
    gather_subset: ${1:['!config']}                                        # When supplied, this argument will restrict the facts collected to a given subset   Possible values for this argument inc
