# -*- mode: snippet -*-
# name: Runs the discovery program I(facter) on the remote system
# prefix: facter_snippet
# key: facter
# group: ansible
# --

- name: ${0:Runs the discovery program I(facter) on the remote system}
  facter:                                                                  
